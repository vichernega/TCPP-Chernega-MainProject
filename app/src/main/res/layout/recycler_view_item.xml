<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/colorPrimary"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/recycler_item_photo"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:civ_border_width="1dp"
        app:civ_border_color="@color/colorWhite"
        android:src="@drawable/ic_launcher_foreground"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/recycler_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text = "Some Name"
        android:textSize="18sp"
        android:textColor="@color/colorWhite"
        android:layout_margin="10dp"
        app:layout_constraintTop_toTopOf="@+id/recycler_item_photo"
        app:layout_constraintStart_toEndOf="@+id/recycler_item_photo"
        app:layout_constraintBottom_toBottomOf="@+id/recycler_item_photo"
        />


</androidx.constraintlayout.widget.ConstraintLayout>